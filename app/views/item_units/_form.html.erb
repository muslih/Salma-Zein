<%= form_for(item_unit) do |f| %>
  <% if item_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_unit.errors.count, "error") %> prohibited this item_unit from being saved:</h2>

      <ul>
      <% item_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Satuan Item" %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit "Simpan" %>
  </div>
<% end %>
