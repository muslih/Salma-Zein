<p id="notice"><%= notice %></p>

<h1>Religions</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @religions.each do |religion| %>
      <tr>
        <td><%= religion.name %></td>
        <td><%= link_to 'Edit', edit_religion_path(religion) %></td>
        <td><%= link_to 'Hapus', religion, method: :delete, data: { confirm: 'Yakin hapus data?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Religion', new_religion_path %>
